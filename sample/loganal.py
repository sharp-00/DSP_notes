import csv


timestamp = []
with open("events.log") as f:
    reader = csv.reader(f)
    rows = list(reader)

    for rows in f:
        timestamp = 
        timpestamp.append(timestamp)
    print(timestamp)
        
